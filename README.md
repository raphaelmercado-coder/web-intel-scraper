# phase3automations

Trigger.dev v3 (TypeScript, Cloud) project for Phase 3 automations.

## Local development

```bash
npm install
npx trigger.dev@latest dev
```

Tasks live under `src/trigger/`. Configuration is in `trigger.config.ts`.
